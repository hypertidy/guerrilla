% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{grid_idw}
\alias{grid_idw}
\title{Inverse distance weighted interpolation}
\usage{
grid_idw(x, value = NULL, grid = NULL, idp = 2, nmax = Inf, ...)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{idp}{the inverse distance power}

\item{nmax}{use at most this many of the nearest points per cell}

\item{...}{passed to \code{\link[gstat:idw]{gstat::idw()}}}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Estimate each cell as a weighted average of the input values, weighted by
one over distance to the power \code{idp}.
}
\details{
There is no model here, only a rule, and that is the point of having it next
to the others. Raising \code{idp} makes the nearest point dominate, so the
surface tends to nearest neighbour (\code{\link[=grid_voronoi]{grid_voronoi()}}); lowering it flattens
towards the overall mean. Neither end is more correct than the other, and
nothing in the data tells you where to sit between them.

The consequence is that this method cannot report a standard error, because
it never claimed to be estimating anything. \code{\link[=grid_kriging]{grid_kriging()}} is the same
idea taken seriously: weights chosen from a fitted model of how the values
vary with distance, which then also says how uncertain each cell is.
}
\examples{
xy <- cbind(runif(50), runif(50))
if (requireNamespace("gstat", quietly = TRUE)) {
  op <- par(mfrow = c(1, 2))
  plot(grid_idw(xy, xy[, 1], idp = 0.5), main = "idp = 0.5")
  plot(grid_idw(xy, xy[, 1], idp = 8), main = "idp = 8")
  par(op)
}
}

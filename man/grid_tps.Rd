% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{grid_tps}
\alias{grid_tps}
\title{Thin plate spline interpolation}
\usage{
grid_tps(
  x,
  value = NULL,
  grid = NULL,
  statistic = c("prediction", "se"),
  lon.lat = FALSE,
  ...
)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{statistic}{\code{"prediction"} for the fitted surface, \code{"se"} for its
standard error}

\item{lon.lat}{the coordinates are longitude and latitude, so distances
should be great circle ones; passed to \code{\link[fields:Tps]{fields::Tps()}}}

\item{...}{passed to \code{\link[fields:Tps]{fields::Tps()}}, for instance \code{lambda} or \code{m}}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Fit a thin plate spline to the points with \code{\link[fields:Tps]{fields::Tps()}} and evaluate it
at every cell.
}
\details{
A thin plate spline is the surface that passes near the data while bending
as little as possible, with a smoothing parameter deciding the trade between
those two. It is the engine behind GDAL's \code{-tps} warping, so this is also
what happens when you georeference an image from ground control points.

Unlike \code{\link[=grid_barycentric]{grid_barycentric()}} it fills the whole grid, including cells far
outside the data, where the answer is extrapolation and should be read as
such. \code{statistic = "se"} is how it tells you that: the standard error grows
with distance from the data, so the two surfaces together say both what the
method thinks and where it is guessing.

\code{lon.lat} is the one place in this package where what the coordinates mean
changes the arithmetic. A spline bends in the plane of its coordinates, and
a degree of longitude is not a degree of latitude anywhere but the equator.
It is not guessed from the \code{crs}, because the guess would be wrong exactly
when it mattered; say what you have.
}
\examples{
xy <- cbind(runif(60), runif(60))
v <- sin(xy[, 1] * 6) + xy[, 2]
if (requireNamespace("fields", quietly = TRUE)) {
  plot(grid_tps(xy, v))
  ## and where it is guessing
  plot(grid_tps(xy, v, statistic = "se"))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{as.matrix.guerrilla_grid}
\alias{as.matrix.guerrilla_grid}
\title{Turn a grid into a matrix}
\usage{
\method{as.matrix}{guerrilla_grid}(x, ...)
}
\arguments{
\item{x}{a \code{guerrilla_grid}}

\item{...}{ignored}
}
\value{
A matrix with \code{nrow} rows and \code{ncol} columns.
}
\description{
The values as a matrix, one row per grid row, with the top row of the grid
first. This is the arrangement \code{image()} and \code{raster} both think in, and it
is where most orientation bugs come from, so it gets its own function.
}
\examples{
g <- grid_spec(dimension = c(3, 2), extent = c(0, 3, 0, 2))
g$values <- 1:6
as.matrix(g)
}

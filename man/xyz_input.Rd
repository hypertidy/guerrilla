% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/input.R
\name{xyz_input}
\alias{xyz_input}
\title{Coordinates and values, from whatever you have}
\usage{
xyz_input(x, value = NULL)
}
\arguments{
\item{x}{coordinates: a matrix, a data frame, or anything \pkg{wk} handles}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}
}
\value{
A list of \code{xy} (a two column matrix), \code{value}, and \code{crs}.
}
\description{
Every interpolation function here takes the same thing: places, and a value
at each place. This is what turns the various ways of expressing that into
one matrix of coordinates and one vector of values.
}
\details{
A value can arrive two ways, and they are the same thing said differently:
as a separate \code{value} argument, or as the z of three dimensional
coordinates. Barycentric interpolation really is treating the value as a
height above the plane, so \code{wk::xyz()} input is not an abuse of notation
here, it is the notation. For kriging or a GAM it would be, which is why
\code{value} exists too.

Objects from \pkg{wk} and \pkg{sf} go through \code{\link[wk:wk_coords]{wk::wk_coords()}}, which also
carries their coordinate reference system across. A plain matrix or data
frame is read by position: first column x, second y, third z if present.
\code{wk::as_xy()} is deliberately not used for those, because it refuses to
guess at column names, and \code{Lon}/\code{Lat} is exactly what real data is called.
}
\examples{
xyz_input(cbind(1:3, 4:6), c(10, 20, 30))

## z is the value
xyz_input(cbind(1:3, 4:6, c(10, 20, 30)))

## and the crs comes along
xyz_input(wk::xyz(1:3, 4:6, c(10, 20, 30), crs = "EPSG:4326"))$crs
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bin.R
\name{grid_bin}
\alias{grid_bin}
\title{Bin values into grid cells}
\usage{
grid_bin(x, value = NULL, grid = NULL, fun = mean, ...)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{fun}{function used to combine the values in one cell}

\item{...}{ignored}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Assign each point to the cell that contains it, and combine the values that
land in the same cell.
}
\details{
This is the simplest thing that can be called interpolation, and it is not
really interpolation at all: nothing is estimated between the points, cells
with no point in them stay \code{NA}, and the answer depends entirely on how big
the cells are. It is here because it is the honest baseline, and because the
question it raises -- what should happen when two points land in one cell --
is a question every other method answers silently.

\code{fun} is that answer, made explicit. \code{mean} averages them, \code{length} counts
them, \code{function(x) x[1]} keeps the first, and looking at \code{grid_bin(fun = length)} next to any other method is the quickest way to see where a surface
is supported by data and where it is supported by the algorithm.

Points on the outer edge of the extent are outside the last cell and are
dropped, which is one reason to pad a grid; see \code{\link[=grid_spec]{grid_spec()}}.

The whole implementation is \code{\link[vaster:cell_from_xy]{vaster::cell_from_xy()}} and \code{\link[=tapply]{tapply()}}. No
raster class is involved, because none is needed.
}
\examples{
xy <- cbind(runif(300), runif(300))
g <- grid_spec(xy, dimension = c(20, 20))
plot(grid_bin(xy, xy[, 1], g))

## how many points went into each cell
plot(grid_bin(xy, xy[, 1], g, fun = length))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{grid_spec}
\alias{grid_spec}
\title{A grid, as a plain list}
\usage{
grid_spec(
  x = NULL,
  dimension = c(60L, 50L),
  extent = NULL,
  crs = NULL,
  pad = 0
)
}
\arguments{
\item{x}{coordinates to cover: anything with two or more columns, such as a
matrix or data frame. Ignored when \code{extent} is given.}

\item{dimension}{number of columns and rows, in that order}

\item{extent}{outer edges \code{c(xmin, xmax, ymin, ymax)}; taken from \code{x} if not
given}

\item{crs}{coordinate reference system, or \code{NULL} (the default) for none}

\item{pad}{fraction of each axis range to add on every side, so the outermost
input coordinates do not sit exactly on the boundary. \code{0} by default.}
}
\value{
A \code{guerrilla_grid}, which is a list of \code{dimension}, \code{extent}, \code{crs}
and \code{values}.
}
\description{
Describe a regular grid: how many cells across and down, what region of the
plane it covers, and optionally what coordinate system that plane is in.
}
\details{
The value returned is a list. That is deliberate. Every raster in every
package is some version of these few numbers, and a reader who can print the
whole thing has actually been told what a raster is:
\itemize{
\item \code{dimension}, the number of columns and rows, in that order;
\item \code{extent}, the outer edges as \verb{xmin, xmax, ymin, ymax};
\item \code{crs}, a coordinate reference system, or \code{NULL} for none;
\item \code{values}, one number per cell, or \code{NULL} for a grid that is only a target.
}

\code{dimension} and \code{extent} are exactly the first two arguments of the
\pkg{vaster} functions, so \code{grid$dimension} and \code{grid$extent} can be handed
straight to \code{\link[vaster:xy_from_cell]{vaster::xy_from_cell()}} and friends. Cells are numbered left to
right along the top row first, the same order raster and GDAL use.

Nothing here assumes the plane is the Earth. \code{crs} stays \code{NULL} unless you
say otherwise.
}
\examples{
xy <- cbind(c(0, 4, 10), c(0, 2, 5))
grid_spec(xy)

## it really is just a list
str(unclass(grid_spec(xy)))

## give the points some room
grid_spec(xy, pad = 0.05)$extent

## or skip the points entirely
grid_spec(dimension = c(4, 3), extent = c(0, 8, 0, 6))
}

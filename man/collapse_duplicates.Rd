% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{collapse_duplicates}
\alias{collapse_duplicates}
\title{Collapse coordinates that repeat}
\usage{
collapse_duplicates(
  xy,
  value,
  fun = mean,
  quiet = FALSE,
  fun_label = deparse(substitute(fun))
)
}
\arguments{
\item{xy}{coordinates, two columns}

\item{value}{one value per row of \code{xy}}

\item{fun}{function used to combine the values at a repeated coordinate}

\item{quiet}{do not report how many coordinates were collapsed}

\item{fun_label}{name of \code{fun} to use in that report}
}
\value{
A list of \code{xy} and \code{value}, with no repeated coordinate.
}
\description{
Triangulation has nothing to say about two values at the same place, and
Qhull refuses outright. Real data does this constantly: a station occupied
twice, a transect that crosses its own track, coordinates rounded to a
printed precision.
}
\details{
So decide explicitly. \code{collapse_duplicates()} reduces repeated coordinates to
one, combining their values with \code{fun}, and says how many it collapsed.
}
\examples{
xy <- cbind(c(1, 2, 2, 3), c(1, 2, 2, 3))
collapse_duplicates(xy, c(10, 20, 30, 40))
}

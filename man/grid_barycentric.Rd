% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bilinear_triangulation.R
\name{grid_barycentric}
\alias{grid_barycentric}
\title{Interpolate to a grid across a triangulation}
\usage{
grid_barycentric(
  x,
  value = NULL,
  grid = NULL,
  duplicates = mean,
  engine = c("geometry", "R"),
  ...
)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{duplicates}{function used to combine values at repeated coordinates,
or \code{NULL} to leave them alone}

\item{engine}{\code{"geometry"} for the fast path, \code{"R"} for the readable one}

\item{...}{ignored}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Triangulate the input coordinates, then estimate a value at every cell of a
target grid from the barycentric coordinates of the cell centre within the
triangle that contains it.
}
\details{
Within a triangle the surface is the plane through its three corner values,
so the result is continuous, passes exactly through the input values, and
invents nothing beyond them. This is what MATLAB calls
\code{griddata(method = "linear")} and what GDAL's \code{gdal_grid} calls \code{linear}.

Cells outside the convex hull of the points are in no triangle, and are left
\code{NA}. Points sharing a coordinate are combined first, because a triangulation
cannot hold two values in one place; see \code{\link[=collapse_duplicates]{collapse_duplicates()}}.
}
\section{Two engines}{

\code{engine = "geometry"} locates the cells and computes their weights in one
pass of \code{geometry::tsearch()}, in C. It is the one to use.

\code{engine = "R"} does the same work in the open: \code{\link[=find_triangle]{find_triangle()}} to locate
each cell, \code{\link[=bary_weights]{bary_weights()}} to weight it. It is slower, and it is the point
of this package -- the two agree to floating point, and the second one can be
read.
}

\examples{
xy <- cbind(runif(100), runif(100))
grid_barycentric(xy, xy[, 1] + xy[, 2])

## the value can be the z of the coordinates
grid_barycentric(cbind(xy, xy[, 1]))

## the two engines agree
g1 <- grid_barycentric(xy, xy[, 1], grid_spec(xy, dimension = c(20, 20)))
g2 <- grid_barycentric(xy, xy[, 1], grid_spec(xy, dimension = c(20, 20)),
                       engine = "R")
max(abs(g1$values - g2$values), na.rm = TRUE)
}

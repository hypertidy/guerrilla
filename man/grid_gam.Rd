% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models.R
\name{grid_gam}
\alias{grid_gam}
\title{Generalized additive model on the coordinates}
\usage{
grid_gam(
  x,
  value = NULL,
  grid = NULL,
  formula = value ~ s(x, y),
  statistic = c("prediction", "se"),
  ...
)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{formula}{a model formula in \code{value}, \code{x} and \code{y}}

\item{statistic}{\code{"prediction"} for the fitted surface, \code{"se"} for its
standard error}

\item{...}{passed to \code{\link[mgcv:gam]{mgcv::gam()}}}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Fit a smooth of x and y with \code{\link[mgcv:gam]{mgcv::gam()}} and predict it at every cell.
}
\details{
The default formula is \code{value ~ s(x, y)}, one isotropic two dimensional
smooth. That matters: \code{mgcv}'s default basis for a two dimensional \code{s()} is a
thin plate regression spline, so this is \code{\link[=grid_tps]{grid_tps()}} with the number of
basis functions reduced and the smoothing parameter chosen by REML. The two
give visibly similar surfaces on the same data, and seeing that is worth more
than either surface on its own.

\code{value ~ s(x) + s(y)} is the wrong model for a surface and a useful thing to
try: additive in x and y means every column of the grid has the same shape as
every other, so it cannot represent a feature that sits in one place. Pass it
as \code{formula} and look.
}
\examples{
xy <- cbind(runif(200), runif(200))
v <- exp(-20 * ((xy[, 1] - 0.5)^2 + (xy[, 2] - 0.5)^2))
if (requireNamespace("mgcv", quietly = TRUE)) {
  op <- par(mfrow = c(1, 2))
  plot(grid_gam(xy, v), main = "s(x, y)")
  ## additive in x and y cannot put a bump in one place
  plot(grid_gam(xy, v, formula = value ~ s(x) + s(y)), main = "s(x) + s(y)")
  par(op)
}
}

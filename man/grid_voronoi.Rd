% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tessellate.R
\name{grid_voronoi}
\alias{grid_voronoi}
\title{Nearest neighbour interpolation, by Voronoi tessellation}
\usage{
grid_voronoi(x, value = NULL, grid = NULL, duplicates = mean, ...)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{duplicates}{function used to combine values at repeated coordinates,
or \code{NULL} to leave them alone}

\item{...}{ignored}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Give every cell the value of the closest input point.
}
\details{
The tessellation is the picture of that rule. A Voronoi tile is exactly the
region closer to its own point than to any other, so assigning each cell to
the tile it falls in is nearest neighbour, drawn rather than computed.

Unlike \code{\link[=grid_barycentric]{grid_barycentric()}}, this fills the whole grid. Voronoi tiles cover
the plane, so there is no convex hull to fall outside of -- which is
convenient and is also the method's main way of lying to you, since a cell
far beyond the data gets a confident answer from one distant point.
}
\examples{
xy <- cbind(runif(30), runif(30))
plot(grid_voronoi(xy, xy[, 1]))
points(xy, pch = 16, cex = 0.5)
}

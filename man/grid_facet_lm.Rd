% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tessellate.R
\name{grid_facet_lm}
\alias{grid_facet_lm}
\title{Fit a plane within each Delaunay triangle}
\usage{
grid_facet_lm(x, value = NULL, grid = NULL, duplicates = mean, ...)
}
\arguments{
\item{x}{coordinates, or coordinates carrying their value as z; see
\code{\link[=xyz_input]{xyz_input()}}}

\item{value}{one value per coordinate, or \code{NULL} to use the z of \code{x}}

\item{grid}{a \code{\link[=grid_spec]{grid_spec()}} to interpolate onto, or \code{NULL} for a default one}

\item{duplicates}{function used to combine values at repeated coordinates,
or \code{NULL} to leave them alone}

\item{...}{ignored}
}
\value{
A \code{guerrilla_grid} with values.
}
\description{
Triangulate the points, fit \code{value ~ x + y} by least squares to the three
corners of each triangle, and evaluate that fit at the cells inside it.
}
\details{
This is the long way round to \code{\link[=grid_barycentric]{grid_barycentric()}}, and the two agree to
floating point. Three points determine a plane, least squares through three
points is that plane, and barycentric interpolation evaluates that same
plane -- so the expensive per-triangle \code{lm()} and the one vectorised
\code{tsearch()} call compute the same surface. Worth seeing once.

It replaces the Delaunay half of \code{facets()}, which did this through
\pkg{spatstat} and returned a point pattern rather than a grid.

The Voronoi half of \code{facets()} is not here, because it was not doing what it
looked like it was doing. A Voronoi tile contains exactly one point by
construction, so \code{lm(value ~ x + y)} on it fits an intercept and nothing
else, and predicts that one point's value across the whole tile. That is
nearest neighbour, computed the slowest way available. \code{\link[=grid_voronoi]{grid_voronoi()}} is
the same answer under its right name.
}
\examples{
xy <- cbind(runif(40), runif(40))
v <- xy[, 1] + xy[, 2]
plot(grid_facet_lm(xy, v))
}
\seealso{
\code{\link[=grid_barycentric]{grid_barycentric()}} for the fast form of this, \code{\link[=grid_voronoi]{grid_voronoi()}}
for what the Voronoi case really was.
}
